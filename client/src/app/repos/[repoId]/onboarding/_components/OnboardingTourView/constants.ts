/* Literals for the onboarding tour screen. Section titles are NOT here: they
   come from the server with the tour, so the TOC and the stored document can
   never disagree about what a section is called. */

/** How long the "Link copied" confirmation stays up, in ms. */
export const COPIED_MS = 2000;
